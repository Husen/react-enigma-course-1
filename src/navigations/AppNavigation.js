import {Route, Routes} from "react-router-dom";
import constants from "../constants";

const {ROUTES} = constants;

const AppNavigation = () => {
    return (
        <Routes>
            <Route path={ROUTES.}
        </Routes>
    )
}
